import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personal.openwebui',
  appName: 'open-webui',
  webDir: 'build' // Load the bundled web assets
};

export default config;
